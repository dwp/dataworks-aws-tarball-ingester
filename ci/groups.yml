groups:
  - name: master
    jobs:
      - development
      - qa
      - integration
      - preprod
      - production
  - name: pull-request
    jobs:
      - dataworks-aws-tarball-ingester-pr
  - name: mirror-minio
    jobs:
      - mirror-minio
